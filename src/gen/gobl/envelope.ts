// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/envelope.json

import type * as dsig from '../dsig/index.js';
import type * as head from '../head/index.js';
import type * as schema from '../schema/index.js';

/** Envelope wraps around a document adding headers and digital signatures. */
export interface Envelope<T extends object = schema.Object> {
  /**
   * Schema identifies the schema that should be used to understand this document
   *
   * @title JSON Schema ID
   */
  $schema: string;
  /**
   * Details on what the contents are
   *
   * @title Header
   */
  head: head.Header;
  /**
   * The data inside the envelope
   *
   * @title Document
   */
  doc: T;
  /**
   * JSON Web Signatures of the header
   *
   * @title Signatures
   */
  sigs?: dsig.Signature[];
}

export const EnvelopeSchemaID = 'https://gobl.org/draft-0/envelope';
