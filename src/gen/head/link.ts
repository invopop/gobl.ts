// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/head/link.json

import type * as cbc from '../cbc/index.js';
import type * as dsig from '../dsig/index.js';
import type { EnumLabels } from '../label.js';

/**
 * Category helps classify the link according to a fixed list. This is optional
 * but highly recommended as it helps receivers better understand the purpose
 * of the link and potentially how it should be presented.
 *
 * @title Category
 */
export type LinkCategory =
  'format' | 'portal' | 'request' | 'response' | 'agreement' | 'verification' | 'attachment';

export const LinkCategoryLabels: EnumLabels<LinkCategory> = {
  format: {
    title: 'Format',
    description: 'Alternative formats of the same document, such as PDF, HTML, or XML.',
  },
  portal: {
    title: 'Portal',
    description:
      'Websites that provide access to alternative versions of this document or request\nmodifications. May also allow access to other or previous business documents.',
  },
  request: {
    title: 'Request',
    description: 'Documents related to requests submitted to other systems.',
  },
  response: {
    title: 'Response',
    description: 'Response documents sent from third party systems often in reply to requests.',
  },
  agreement: {
    title: 'Agreement',
    description: 'Contracts or agreements related to this document.',
  },
  verification: {
    title: 'Verification',
    description:
      'Evidence or verification documents that can be used to validate the authenticity\nof this document.',
  },
  attachment: {
    title: 'Attachment',
    description:
      'General attachments for related information such as spread sheets, letters,\npresentations, specifications, etc.',
  },
};

/** Link defines a link between this document and another resource. */
export interface Link {
  /**
   * Universally Unique Identifier.
   *
   * @title UUID
   * @format uuid
   */
  uuid?: string;
  /**
   * Category helps classify the link according to a fixed list. This is optional
   * but highly recommended as it helps receivers better understand the purpose
   * of the link and potentially how it should be presented.
   *
   * @title Category
   */
  category?: LinkCategory;
  /**
   * Key is a unique identifier for the link within the header and category
   * if provided.
   */
  key: cbc.Key;
  /**
   * Code used to identify the contents of the link.
   *
   * @title Code
   */
  code?: cbc.Code;
  /**
   * Title of the resource to use when presenting to users.
   *
   * @title Title
   */
  title?: string;
  /**
   * Description of the resource to use when presenting to users.
   *
   * @title Description
   */
  description?: string;
  /**
   * Expected MIME type of the link's content when the content is a file. Can only
   * be one of the allowed types defined by EN 16931-1:2017 plus XML itself.
   *
   * @title MIME Type
   * @format mime
   */
  mime?: string;
  /**
   * Digest is used to verify the integrity of the destination document
   * when downloaded from the URL.
   *
   * @title Digest
   */
  digest?: dsig.Digest;
  /**
   * URL of the resource.
   *
   * @title URL
   * @format uri
   */
  url: string;
  /**
   * Meta contains additional metadata about the link.
   *
   * @title Metadata
   */
  meta?: cbc.Meta;
}

export const LinkSchemaID = 'https://gobl.org/draft-0/head/link';
