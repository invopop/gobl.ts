// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/tax/set.json

import type * as cbc from '../cbc/index.js';
import type * as l10n from '../l10n/index.js';
import type * as num from '../num/index.js';
import type { Extensions } from './extensions.js';

/**
 * Combo represents the tax combination of a category code and rate key.
 *
 * @remarks The source schema further restricts some property values conditionally (JSON Schema if/then). That constraint is not expressible in TypeScript and is not encoded here.
 */
export interface Combo {
  /**
   * Tax category code from those available inside a region.
   *
   * @title Category
   */
  cat: cbc.Code;
  /**
   * Country code override when issuing with taxes applied from different countries.
   *
   * @title Country
   */
  country?: l10n.TaxCountryCode;
  /** Key helps determine the tax situation within the category. */
  key?: cbc.Key;
  /**
   * Rate within a category and for a given key to apply.
   *
   * @title Rate
   */
  rate?: cbc.Key;
  /**
   * Percent defines the percentage set manually or determined from the
   * key. A nil percent implies that this tax combo is either exempt or not-subject.
   *
   * @title Percent
   * @calculated Computed by GOBL; omit when building a document.
   */
  percent?: num.Percentage;
  /**
   * Some countries require an additional surcharge (may be determined if key present).
   *
   * @title Surcharge
   * @calculated Computed by GOBL; omit when building a document.
   */
  surcharge?: num.Percentage;
  /**
   * Local codes that apply for a given rate or percentage that need to be identified and validated.
   *
   * @title Extensions
   */
  ext?: Extensions;
}

/** Properties of {@link Combo} that GOBL computes during calculation. */
export type ComboCalculatedKeys = 'percent' | 'surcharge';

export const ComboSchemaID = 'https://gobl.org/draft-0/tax/set#/$defs/tax.Combo';

/** Set defines a list of tax categories and their rates to be used alongside taxable items. */
export type Set = Combo[];

export const SetSchemaID = 'https://gobl.org/draft-0/tax/set';
