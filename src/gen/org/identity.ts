// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/org/identity.json

import type * as cbc from '../cbc/index.js';
import type * as l10n from '../l10n/index.js';
import type { EnumLabels } from '../label.js';
import type * as tax from '../tax/index.js';

/**
 * Scope defines the context in which this identity is meant to be used.
 *
 * @title Scope
 */
export type IdentityScope = 'tax' | 'legal';

export const IdentityScopeLabels: EnumLabels<IdentityScope> = {
  tax: {
    title: 'Tax',
  },
  legal: {
    title: 'Legal',
  },
};

/** Identity is used to define a code for a specific context. */
export interface Identity {
  /**
   * Universally Unique Identifier.
   *
   * @title UUID
   * @format uuid
   */
  uuid?: string;
  /**
   * Optional label useful for non-standard identities to give a bit more context.
   *
   * @title Label
   */
  label?: string;
  /**
   * Scope defines the context in which this identity is meant to be used.
   *
   * @title Scope
   */
  scope?: IdentityScope;
  /**
   * Country from which the identity was issued.
   *
   * @title Country
   */
  country?: l10n.ISOCountryCode;
  /**
   * Uniquely classify this identity using a key instead of a Type.
   *
   * @title Key
   */
  key?: cbc.Key;
  /**
   * The type of Code being represented and usually specific for
   * a particular context, country, or tax regime, and cannot be used
   * alongside the key.
   *
   * @title Type
   */
  type?: cbc.Code;
  /**
   * The actual value of the identity code.
   *
   * @title Code
   */
  code: cbc.Code;
  /**
   * Description adds details about what the code could mean or imply
   *
   * @title Description
   */
  description?: string;
  /**
   * Ext provides a way to add additional information to the identity.
   *
   * @title Extensions
   */
  ext?: tax.Extensions;
}

export const IdentitySchemaID = 'https://gobl.org/draft-0/org/identity';
