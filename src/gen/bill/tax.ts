// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/bill/tax.json

import type * as cbc from '../cbc/index.js';
import type { EnumLabels } from '../label.js';
import type * as tax from '../tax/index.js';

/**
 * Rounding model used to perform tax calculations on the invoice. This
 * will be configured automatically based on the tax regime, or
 * `sum-then-round` by default, but you can override here if needed.
 * Use with caution, as some conversion tools may make assumptions about
 * the rounding model used.
 *
 * @title Rounding Model
 */
export type TaxRounding = 'precise' | 'currency';

export const TaxRoundingLabels: EnumLabels<TaxRounding> = {
  precise: {
    title: 'Precise',
    description:
      'The default method of calculating the totals in GOBL, and provides the best results\nfor most cases as the precision is maintained to the maximum amount possible. The\ntradeoff however is that sometimes the totals may not sum exactly based on what is visible.',
  },
  currency: {
    title: 'Currency',
    description:
      "The alternative method of calculating the totals that will first round all the amounts\nto the currency's precision before making the sums. Totals using this approach can always\nbe recalculated using the amounts presented, but can lead to rounding errors in the case\nof pre-payments. When prices include tax, the tax amounts are calculated from each rate's\nsum of tax-inclusive line totals so that the bases and amounts always add up, though\nrecalculating a tax amount from its base alone may occasionally differ by a single\nsubunit of the currency.",
  },
};

/**
 * Point is a code that identifies the event which triggers the tax liability,
 * such as invoice issuance, delivery of goods, or receipt of payment.
 *
 * @title Point
 */
export type TaxPoint = 'issue' | 'delivery' | 'payment';

export const TaxPointLabels: EnumLabels<TaxPoint> = {
  issue: {
    title: 'Issue Date',
    description: 'Tax point is the invoice issue date. Corresponds to UNCL 2005 code 3.',
  },
  delivery: {
    title: 'Delivery Date',
    description: 'Tax point is the actual delivery date. Corresponds to UNCL 2005 code 35.',
  },
  payment: {
    title: 'Payment Date',
    description:
      'Tax point is the date on which payment is made. Corresponds to UNCL 2005 code 432.',
  },
};

/** Tax defines a summary of the taxes which may be applied to an invoice. */
export interface Tax {
  /**
   * Category of the tax already included in the line item prices, especially
   * useful for B2C retailers with customers who prefer final prices inclusive of
   * tax.
   *
   * @title Prices Include
   */
  prices_include?: cbc.Code;
  /**
   * Rounding model used to perform tax calculations on the invoice. This
   * will be configured automatically based on the tax regime, or
   * `sum-then-round` by default, but you can override here if needed.
   * Use with caution, as some conversion tools may make assumptions about
   * the rounding model used.
   *
   * @title Rounding Model
   */
  rounding?: TaxRounding;
  /**
   * Point is a code that identifies the event which triggers the tax liability,
   * such as invoice issuance, delivery of goods, or receipt of payment.
   *
   * @title Point
   */
  point?: TaxPoint;
  /**
   * Additional extensions that are applied to the invoice as a whole as opposed to specific
   * sections.
   *
   * @title Extensions
   */
  ext?: tax.Extensions;
  /**
   * Notes contains tax-related notes, typically used for exemption reasons
   * or other tax-specific explanations associated with particular tax categories.
   *
   * @title Notes
   */
  notes?: tax.Note[];
  /**
   * Any additional data that may be required for processing, but should never
   * be relied upon by recipients.
   *
   * @title Meta
   */
  meta?: cbc.Meta;
}

export const TaxSchemaID = 'https://gobl.org/draft-0/bill/tax';
