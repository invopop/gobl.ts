// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/bill/status.json

import type * as cal from '../cal/index.js';
import type * as cbc from '../cbc/index.js';
import type { EnumLabels } from '../label.js';
import type * as org from '../org/index.js';
import type * as tax from '../tax/index.js';
import type { Ordering } from './ordering.js';
import type { StatusLine } from './status-line.js';

/**
 * Type of status being reported (e.g. "system" for internal events).
 *
 * @title Type
 */
export type StatusType = 'response' | 'update' | 'system';

export const StatusTypeLabels: EnumLabels<StatusType> = {
  response: {
    title: 'Response',
    description:
      'A response to a document that has been submitted, often used in a two way communication between a customer and supplier.',
  },
  update: {
    title: 'Update',
    description:
      'Issued by the supplier or seller that will be shared with either the customer, a fifth-corner entity such as a government agency, or both.',
  },
  system: {
    title: 'System',
    description:
      'A system event that is not directly related to a specific document but needs to be recorded for compliance purposes.',
  },
};

/**
 * Status represents a system or business event that needs to be recorded for compliance purposes.
 */
export interface Status {
  /**
   * Country code that identifies the tax regime applicable to the document.
   * It determines which country-specific tax rules, normalizations, and validations are applied.
   * It may be determined automatically via normalization of a supplier or issuer tax identity
   * country code.
   *
   * @title Tax Regime
   * @recommended
   */
  $regime?: tax.RegimeCode;
  /**
   * Addons defines a list of keys used to identify tax addons that apply special
   * normalization, scenarios, and validation rules to a document.
   *
   * @title Addons
   */
  $addons?: tax.AddonList;
  /**
   * Universally Unique Identifier.
   *
   * @title UUID
   * @format uuid
   */
  uuid?: string;
  /**
   * Type of status being reported (e.g. "system" for internal events).
   *
   * @title Type
   */
  type: StatusType;
  /**
   * IssueDate is the date when the status is to be considered effective.
   *
   * @title Issue Date
   */
  issue_date: cal.Date;
  /**
   * IssueTime is used when extra precision is required to determine when exactly
   * the status was issued.
   *
   * @title Issue Time
   */
  issue_time?: cal.Time;
  /**
   * Series is an optional code to group related status events together.
   *
   * @title Series
   * @recommended
   */
  series?: cbc.Code;
  /**
   * Code provides a way to identify the specific status event being reported.
   *
   * @title Code
   */
  code: cbc.Code;
  /**
   * Ext provides additional structured data specific to the regime or addon.
   *
   * @title Extensions
   */
  ext?: tax.Extensions;
  /**
   * Supplier represents the entity supplying the goods or services in the
   * original transaction.
   *
   * @title Supplier
   */
  supplier: org.Party;
  /**
   * Customer is optional and describes the recipient of the original
   * services.
   *
   * @title Customer
   */
  customer?: org.Party;
  /**
   * Ordering provides links to related documents and additional details about
   * which parties may be involved in the transaction.
   *
   * @title Ordering
   */
  ordering?: Ordering;
  /**
   * Lines contain the main payload of the message used to describe individual
   * documents which have a status.
   *
   * @title Lines
   * @recommended
   */
  lines: StatusLine[];
  /**
   * Notes for additional details about the event.
   *
   * @title Notes
   */
  notes?: org.Note[];
  /**
   * Meta contains unstructured data useful for internal tools.
   *
   * @title Meta
   */
  meta?: cbc.Meta;
}

export const StatusSchemaID = 'https://gobl.org/draft-0/bill/status';
