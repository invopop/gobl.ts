// Code generated by cmd/generate. DO NOT EDIT.
// Source: schemas/bill/status-line.json

import type * as cal from '../cal/index.js';
import type * as cbc from '../cbc/index.js';
import type { EnumLabels } from '../label.js';
import type * as org from '../org/index.js';
import type * as schema from '../schema/index.js';
import type * as tax from '../tax/index.js';
import type { Action } from './action.js';
import type { Reason } from './reason.js';

/**
 * Status Key indicates the situation of the document
 *
 * @title Key
 */
export type StatusLineKey =
  | 'issued'
  | 'acknowledged'
  | 'processing'
  | 'querying'
  | 'rejected'
  | 'accepted'
  | 'paid'
  | 'error'
  | 'other';

export const StatusLineKeyLabels: EnumLabels<StatusLineKey> = {
  issued: {
    title: 'Issued',
    description: 'Document has been submitted pending review by the recipient.',
  },
  acknowledged: {
    title: 'Acknowledged',
    description:
      'Received a readable invoice message that can be understood and submitted for processing by the Buyer.',
  },
  processing: {
    title: 'In Process',
    description: 'Indicates that the referenced message or transaction is being processed.',
  },
  querying: {
    title: 'Under Query',
    description:
      'Buyer will not proceed to accept the Invoice without receiving additional information from the Seller.',
  },
  rejected: {
    title: 'Rejected',
    description:
      'Buyer will not process the referenced Invoice any further. Buyer is rejecting this invoice but not necessarily the commercial transaction. Although it can be used also for rejection for commercial reasons (invoice not corresponding to delivery).',
  },
  accepted: {
    title: 'Accepted / Approved',
    description: 'Buyer has given a final approval of the invoice and the next step is payment.',
  },
  paid: {
    title: 'Paid',
    description:
      'Buyer has initiated payment, or the supplier has acknowledged receipt of payment.',
  },
  error: {
    title: 'Error',
    description: 'There is a technical issue with the document that has caused it to be rejected.',
  },
  other: {
    title: 'Other',
    description: 'Status to be determined by other codes or details listed in the line.',
  },
};

/**
 * StatusLine represents a single row inside a message, that describes the situation of another business document.
 */
export interface StatusLine {
  /**
   * Position of the row inside the message, determined automatically.
   *
   * @title Index
   */
  index: number;
  /**
   * Status Key indicates the situation of the document
   *
   * @title Key
   */
  key: StatusLineKey;
  /**
   * When this row should be considered effective
   *
   * @title Date
   */
  date?: cal.Date;
  /**
   * Document reference or details about the document that needs to be looked
   * at.
   *
   * @title Document
   */
  doc?: org.DocumentRef;
  /**
   * Description includes a human readable description that explains the
   * reason for the current status, if necessary.
   *
   * @title Description
   */
  description?: string;
  /**
   * Reasons define an array of reason objects that help the recipient
   * determine why the status was provided.
   *
   * @title Reasons
   */
  reasons?: Reason[];
  /**
   * Actions contains an array of actions that should be carried out by the
   * recipient of the message. These are suggestions.
   *
   * @title Actions
   */
  actions?: Action[];
  /**
   * Extensions for local or format focussed data
   *
   * @title Extensions
   */
  ext?: tax.Extensions;
  /**
   * Complements contain regime/addon specific payload data.
   *
   * @title Complements
   */
  complements?: schema.Object[];
  /**
   * Additional data specific for the source system.
   *
   * @title Meta
   */
  meta?: cbc.Meta;
}

export const StatusLineSchemaID = 'https://gobl.org/draft-0/bill/status-line';
